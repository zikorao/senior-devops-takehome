kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: takehome
nodes:
  - role: control-plane
    kubeadmConfigPatches:
      - |
        kind: InitConfiguration
        nodeRegistration:
          kubeletExtraArgs:
            - name: node-labels
              value: ingress-ready=true
    extraPortMappings:
      - containerPort: 80
        hostPort: 8080
        protocol: TCP
